<Page
    x:Class="NewtonContactsApp.MasterRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NewtonContactsApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Add" Label="Add" Click="btn_AppBarAdd_Click" />
            <AppBarSeparator />
            <AppBarButton x:Name="btn_AppBarGrid"
                          Icon="ViewAll" Label="Grid"
                          Click="btn_AppBarView_Click" />
            <AppBarButton x:Name="btn_AppBarList"
                          Icon="List" Label="List"
                          Click="btn_AppBarView_Click" />
        </CommandBar>
    </Page.BottomAppBar>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Frame x:Name="frm_MasterRoot" />
    </Grid>
</Page>
